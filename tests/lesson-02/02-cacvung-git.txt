a. Tạo 3 file: file1, file2, file3
-> Tạo file ở local (vùng working directory)

b. Chạy lệnh: git init
-> Khởi tạo repo local (vùng working directory)

c. Chạy lệnh git commit -m"inint project"
-> Thông baos lỗi do chưa có file naò được add

d. Chạy lệnh: git add file1
-> Commit file1 từ vùng working directory sang vùng STG Area

e. Chạy lệnh git commit -m'add file1'
-> Commit file1 (vùng Local Repository)

f. Chạy lệnh git commit -m'add file'
-> Thông báo lỗi do file chưa được add
